{
  "name": "conduit-tester",
  "version": "1.0.0",
  "private": true,
  "description": "Central test suite that gates PRs for the conduit-backend and conduit-frontend repos.",
  "type": "commonjs",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/api",
    "test:e2e": "playwright test tests/e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.6.0"
  }
}
